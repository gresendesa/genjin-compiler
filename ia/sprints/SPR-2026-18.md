# SPR-2026-18 — B-029: Importação de proc-blocos de arquivos .gnj externos

- Status: concluída
- Data de planejamento: 2026-05-04
- Data de início: 2026-05-04
- Data de encerramento: 2026-05-05
- Owner: agente
- PO: gresendesa

---

## Objetivo

Implementar a instrução `from "mod" import Name1, Name2` dentro de `procs {}`, permitindo importar proc-blocos de arquivos `.gnj` externos. Último recurso necessário para v1.0 oficial da linguagem.

---

## Itens da sprint

| ID | Título | Prioridade PO | Risco |
|---|---|---|---|
| B-028 | Estudo de viabilidade (concluído antes da sprint) | 1 (crítica) | — |
| B-029 | Implementação: importação de proc-blocos de .gnj externos | 1 (crítica) | médio |

**Risco da sprint:** médio (2.0)

---

## Tarefas técnicas

| ID | Descrição | Arquivo | Risco | Status |
|---|---|---|---|---|
| S18-T01 | Scanner: `KW_IMPORT` | `compiler/scanner.py` | baixo | concluído |
| S18-T02 | Parser: `ProcImportNode` + branch em `_parse_procs` + placeholder em `declared_procs_map` | `compiler/parser.py` | médio | concluído |
| S18-T03 | `resolve_imports.py`: nova fase, resolução de path, ciclos, validações | `compiler/resolve_imports.py` | médio | concluído |
| S18-T04 | Pipeline: integrar `resolve_imports` + `--import-base` na CLI | `compiler.py` | baixo | concluído |
| S18-T05 | `ast_io.py`: suporte a `ProcImportNode` na serialização | `compiler/ast_io.py` | baixo | concluído |
| S18-T06 | Testes: scanner, parser, resolve_imports, regressão | `tests/` | médio | concluído |
| S18-T07 | VS Code extension: keyword `import` | `vscode-genjin/syntaxes/genjin.tmLanguage.json` | baixo | concluído |
| S18-T08 | Atualizar `lenhador-sintetico.gnj` | `examples/lenhador-sintetico.gnj` | baixo | concluído |

---

## Decisões técnicas chave

- Pipeline pós-sprint: `Scanner → Parser → ResolveImports → Desugar → Transpiler → Assembler`
- Nó temporário `ProcImportNode` existe apenas entre Parser e ResolveImports
- Placeholder em `declared_procs_map` para importados durante parsing (sem codes) — validação diferida
- Path base: diretório do `.gnj` compilado (padrão) ou `--import-base <dir>`
- Importável: apenas `ProcBlockNode`; procs normais ignorados com aviso

---

## Concluídos

- S18-T01 Scanner: `KW_IMPORT` adicionado ao enum `TokenType` e dict `_KEYWORDS`
- S18-T02 Parser: `ProcImportNode` dataclass; branch `KW_FROM` em `_parse_procs`; placeholders em `_parse_program`
- S18-T03 `compiler/resolve_imports.py` criado: fase 2.7 do pipeline, resolução de path dotted, detecção de ciclos, validação de nomes, importação em cadeia
- S18-T04 Pipeline integrado em `compiler.py`: `resolve_imports` entre `parse` e `desugar`; argumento `--import-base` na CLI
- S18-T05 `ast_io.py`: `ProcImportNode` adicionado ao `_NODE_TYPES`
- S18-T06 Testes: +17 novos testes (scanner, parser, resolve_imports); 434 passando, 0 falhas
- S18-T07 VS Code extension: `import` adicionado ao grupo `keyword.declaration.genjin`
- S18-T08 `examples/lenhador-sintetico.gnj` atualizado com uso real de `from ... import`
- docs/language.md atualizado com seção completa de importação de proc-blocos (resolução de path, CLI, restrições, erros, exemplo)

---

## Pendências e resumo

Sem pendências. Sprint encerrada em 2026-05-05.

**Resultado:** B-029 concluído. Feature de importação de proc-blocos implementada do zero: scanner → parser → nova fase `resolve_imports` → pipeline → CLI → extensão VS Code → exemplos → documentação. Pipeline oficial pós-sprint: `Scanner → Parser → ResolveImports → Desugar → Transpiler → Assembler`. 434 testes, 0 falhas.
