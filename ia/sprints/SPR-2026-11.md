# SPR-2026-11

- Status: ativa
- Data de início: 2026-05-03
- Data de encerramento: (a definir)
- Foco: B-021 — Criar `examples/lenhador.gnj` + fix parser `param: Type[]`
- Owner: agente + gresendesa (PO)

---

## Escopo

| Item | Título | Prioridade PO | Risco | Ordem |
|---|---|---|---|---|
| B-021 | Criar `examples/lenhador.gnj` — script de referência do LenhadorNEO | 1 (crítica) | médio | 1 |

---

## Decisão de contexto

**Regra confirmada pelo PO (2026-05-03):** variáveis do tipo plural (`Type[]`) são sempre
passadas por referência. A notação `&Type[]` é redundante e não deve ser usada na sintaxe `.gnj`.
O compilador deve inferir `evaluation='reference'` automaticamente quando `[]` é detectado em
um parâmetro.

---

## Tarefas técnicas

| Tarefa | Descrição | Risco |
|---|---|---|
| S11-T01 | Verificar compatibilidade da sintaxe atual: scanner non-ASCII, `while` múltiplos, valores `"%&home%"`, `param=&var` plural | baixo |
| S11-T02 | Corrigir parser `_parse_param_decl`: suportar `param: Type[]` com `evaluation='reference'` implícito; documentar regra em `docs/language.md` | baixo |
| S11-T03 | Escrever `examples/lenhador.gnj` — vars (13), procs (14), bloco raiz com `ferramenta_gasta` expandido inline | médio |
| S11-T04 | Testar compilação (`python compiler.py examples/lenhador.gnj`) e validar saída Jinja2 gerada | médio |
| S11-T05 | DoD: regressão (`pytest`), commit, atualizar `ia/` | baixo |

**Risco da sprint: médio (1.6)**

---

## Definition of Done

- [ ] Regra `param: Type[]` = ref implícita documentada em `docs/language.md` (S11-T02)
- [ ] `_parse_param_decl` aceita `Type[]` e define `evaluation='reference'` (S11-T02)
- [ ] `examples/lenhador.gnj` criado e compilando sem erros (S11-T03/T04)
- [ ] Saída Jinja2 semanticamente equivalente ao bloco `programa` do LenhadorNEO (S11-T04)
- [ ] Testes de regressão passando (S11-T05)
- [ ] Incompatibilidades encontradas documentadas como novos itens de backlog (S11-T04/T05)
- [ ] Memória `ia/` atualizada e sprint encerrada (S11-T05)

---

## Ordem de execução

1. S11-T01 — verificação de compatibilidade (pré-requisito para S11-T02 e T03)
2. S11-T02 — fix do parser + doc (pré-requisito para S11-T03)
3. S11-T03 — escrita do lenhador.gnj
4. S11-T04 — teste e validação
5. S11-T05 — DoD e fechamento
