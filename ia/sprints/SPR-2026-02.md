# SPR-2026-02 — Implementação do Pipeline do Compilador

- Status: **concluida**
- Data de início: 2026-04-27
- Data de encerramento: 2026-04-28
- Owner: agente
- Foco: Implementação completa do compilador (.gnj → tokens → AST → Jinja2)
- Risco da sprint: medio (2.33)

---

## Itens recortados

| ID | Titulo | Prioridade PO | Risco | Ordem |
|---|---|---|---|---|
| B-004 | Implementar scanner (`compiler/scanner.py`) | 1 (critica) | medio | 1º |
| B-005 | Implementar parser (`compiler/parser.py`) | 2 (alta) | alto | 2º |
| B-006 | Implementar transpiler (`compiler/transpiler.py`) | 3 (media) | medio | 3º |

### Cálculo de risco da sprint

- B-004: risco 2 (médio)
- B-005: risco 3 (alto)
- B-006: risco 2 (médio)

Média ponderada: (2+3+2) / 3 = **2.33 → médio/alto** (limite: > 2.3 = alto, mas próximo do limite medio)

Classificação adotada: **médio** (2.33 está exatamente no limiar — itens são independentes entre si em risco)

---

## Tarefas técnicas

### B-004 — Scanner

| ID | Tarefa | Risco | Status |
|---|---|---|---|
| S2-T01 | Definir `TokenType` (enum) e `Token` (dataclass) com campo `line` | baixo | concluido |
| S2-T02 | Implementar `Scanner` com reconhecimento de todos os token types | medio | concluido |
| S2-T03 | Tratar `>>` com precedência sobre `>` e ignorar comentários `//` e `/* */` | medio | concluido |
| S2-T04 | Escrever testes unitários do scanner em `tests/` | baixo | concluido |

### B-005 — Parser

| ID | Tarefa | Risco | Status |
|---|---|---|---|
| S2-T05 | Definir todos os nós da AST (dataclasses) | baixo | concluido |
| S2-T06 | Implementar parser recursivo descendente para `program`, `vars`, `procs` | medio | concluido |
| S2-T07 | Implementar parser para blocos `exec` com aninhamento, `case`, `while`, `pass` | alto | concluido |
| S2-T08 | Implementar validações semânticas (códigos, variáveis, parâmetros) | alto | concluido |
| S2-T09 | Escrever testes unitários do parser em `tests/` | medio | concluido |

### B-006 — Transpiler

| ID | Tarefa | Risco | Status |
|---|---|---|---|
| S2-T10 | Implementar geração das seções `import` e declaração de `program` | medio | concluido |
| S2-T11 | Implementar geração do `ATTRIBUTE.BLOCK` com casos, while e pass aninhados | alto | concluido |
| S2-T12 | Escrever teste de integração: `basic.gnj` → template Jinja2 executável | medio | concluido |
| S2-T13 | Executar regressão completa em `tests/` | baixo | concluido |

---

## Ordem de execução

1. S2-T01 → S2-T02 → S2-T03 → S2-T04 (scanner completo e testado)
2. S2-T05 → S2-T06 → S2-T07 → S2-T08 → S2-T09 (parser completo e testado)
3. S2-T10 → S2-T11 → S2-T12 → S2-T13 (transpiler completo e testado)

---

## Bloqueios previstos e mitigações

| Bloqueio | Mitigação |
|---|---|
| Ambiguidade semântica na herança de variável em blocos aninhados | Consultar B-005 e docs/language.md como referência |
| Mapeamento AST → Jinja2 inconsistente com genjin.jinja2 | Validar template gerado executando com assembler.py antes de fechar B-006 |
| Casos de borda do scanner (ex: `>>` vs `>`) | Cobrir explicitamente em S2-T03 e testes |

---

## Fechamento

- **Concluídos:** B-004, B-005, B-006 (todos os 13 itens técnicos S2-T01 a S2-T13)
- **Pendências:** nenhuma
- **Total de testes:** 167 (65 scanner, 51 parser, 42 transpiler, 9 delimiters) — 0 falhas
- **Descobertas relevantes registradas:**
  - `while(...)` fica sintaticamente após `}` do exec, não dentro do corpo
  - `pass` aceita códigos borbulhados de procs filhos (sem validação restritiva)
  - `variable_explicit: bool` adicionado ao `ExecBlockNode` para que o transpiler omita `ATTRIBUTE.VARIABLE` em blocos herdados, delegando a resolução ao runtime do motor Genjin via breadcrumb
  - `ATTRIBUTE.MACRO` gerado como lista `['library', 'macro']` para uso via `{* import library as LIB *}` no motor
- **Resumo:** Sprint entregue completa em uma única sessão (2026-04-27/28). Pipeline funcional: `.gnj → tokens → AST → Jinja2`. Próxima sprint: B-007 (CLI por etapa) + B-008 (compiler.py).
