# SPR-2026-08

- Status: concluída
- Data de início: 2026-05-01
- Data de fechamento: 2026-05-01
- Foco: B-015 Fase 2 — implementação completa da notação inline `@proc()` / `@proc() when(CODE)`
- Owner: agente + gresendesa (PO)

---

## Escopo

| Item | Título | Prioridade PO | Risco | Ordem |
|---|---|---|---|---|
| B-015 (Fase 2) | Implementação: scanner, parser, desugar, compiler.py, testes, docs, examples | 1 (crítica) | alto | 1 |

**Decisões de arquitetura aplicadas:** DEC-001 (desugar separado), DEC-002 (sintaxe `when(CODE)`)

---

## Tarefas técnicas

| Tarefa | Descrição | Risco |
|---|---|---|
| S8-T01 | `compiler/scanner.py` — adicionar tokens `AT` (`@`) e `KW_WHEN` (`when`) | baixo |
| S8-T02 | `compiler/parser.py` — adicionar `InlineAtomNode`, `InlineSeqNode`; parsear inline em posições válidas | alto |
| S8-T03 | `compiler/desugar.py` — implementar fase de desaçucaramento (algoritmo de S7-T02) | alto |
| S8-T04 | `compiler.py` — integrar `desugar()` na pipeline entre parse e transpile | baixo |
| S8-T05 | `tests/scanner/` — testes para `AT` e `KW_WHEN` | baixo |
| S8-T06 | `tests/parser/` — testes para atoms inline e sequências | médio |
| S8-T07 | `tests/desugar/` — testes para expansão canônica (Tipo 1 e Tipo 2) | alto |
| S8-T08 | `docs/language.md` — seção dedicada à notação inline | baixo |
| S8-T09 | `examples/` — adicionar exemplo demonstrando `@proc()` e `@proc() when(CODE)` | baixo |
| S8-T10 | Regressão: executar suite completa de testes | baixo |

---

## Definition of Done

- [x] Todos os testes passam (pytest) — 341 passed, 0 failed
- [x] Tipo 1 (`@proc()`) aceito e expandido corretamente
- [x] Tipo 2 (`@proc() when(CODE)`) aceito e expandido corretamente
- [x] Encadeamento de múltiplos `@` no mesmo `case` funciona
- [x] `while` em anotação `@` tratado corretamente
- [x] `>>` em anotação `@` tratado corretamente
- [x] Checklist de regressão executado (341 testes)
- [x] Memória atualizada em `ia/`
- [x] `docs/language.md` reflete a notação inline

---

## Concluídos

- S8-T01: `compiler/scanner.py` — tokens `AT` e `KW_WHEN` adicionados
- S8-T02: `compiler/parser.py` — `InlineAtomNode`, `InlineSeqNode`, parsing inline
- S8-T03: `compiler/desugar.py` — fase desugar implementada (novo arquivo)
- S8-T04: `compiler.py` — `desugar()` integrado na pipeline
- S8-T05: `tests/scanner/test_scanner.py` — `TestInlineNotation` (8 testes), correções para `@` válido
- S8-T06: `tests/parser/test_parser.py` — `TestInlineParsing` (~20 testes)
- S8-T07: `tests/desugar/test_desugar.py` — suite desugar criada (~35 testes, 6 classes)
- S8-T08: `docs/language.md` — seção "Notação inline (`@proc()`)" adicionada
- S8-T09: `examples/inline.gnj` — exemplo criado com Tipo 1 e Tipo 2
- S8-T10: Regressão — 341 passed, 0 failed (era 287 antes desta sprint)

## Resumo

Sprint concluída com sucesso. Implementação completa do açúcar sintático `@proc()` / `@proc() when(CODE)` (B-015).
Pipeline do compilador: Source → Scanner → Parser → **Desugar** → Transpiler → Assembler.
+54 novos testes. Zero regressões.
