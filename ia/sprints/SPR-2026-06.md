# SPR-2026-06

- Status: em andamento
- Data de início: 2026-05-01
- Foco: Correções de gramática do parser (ordem `as`/`>>`, remoção de `proc`, ordem flexível de blocos)
- Owner: agente + gresendesa (PO)

---

## Escopo

| Item | Título | Prioridade PO | Risco | Ordem |
|---|---|---|---|---|
| B-013 | Parser: remover keyword `proc` das declarações dentro de `procs { }` | 1 (crítica) | baixo | 1 |
| B-012 | Parser: enforçar ordem `as` antes de `>>` no bloco exec | 1 (crítica) | baixo | 2 |
| B-014 | Parser: tornar a ordem dos blocos `vars`, `procs`, `exec` flexível | 1 (crítica) | médio | 3 |

**Risco da sprint:** baixo/médio (1.33)

**Justificativa de ordem:**
- B-013 primeiro: remove `proc` — simplifica a gramática antes das demais mudanças.
- B-012 segundo: corrige a ordem `as`/`>>` — mudança localizada e independente.
- B-014 terceiro: flexibiliza ordem dos blocos — mais complexo (dois passes no parser), deve ser feito com gramática já estabilizada pelos dois anteriores.

---

## Tarefas técnicas

### B-013 — Remover keyword `proc`

| Tarefa | Descrição | Risco |
|---|---|---|
| S6-T01 | `compiler/scanner.py`: remover ou deprecar `KW_PROC` | baixo |
| S6-T02 | `compiler/parser.py`: remover `_expect(KW_PROC)` em `_parse_proc_decl` | baixo |
| S6-T03 | Atualizar `examples/basic.gnj` e `examples/test.gnj` | baixo |
| S6-T04 | Atualizar fixtures de teste que usem sintaxe `proc nome(...)` | baixo |
| S6-T05 | Atualizar `docs/language.md` | baixo |

### B-012 — Enforçar ordem `as` antes de `>>`

| Tarefa | Descrição | Risco |
|---|---|---|
| S6-T06 | `compiler/parser.py`: inverter ordem de parsing (`as` antes de `>>`) em `_parse_exec_block` | baixo |
| S6-T07 | Garantir que `>> var as "nome"` lança `ParseError` com mensagem clara | baixo |
| S6-T08 | Atualizar testes que cubram sintaxe do `exec` | baixo |
| S6-T09 | Verificar e corrigir `examples/` se necessário | baixo |

### B-014 — Ordem flexível dos blocos

| Tarefa | Descrição | Risco |
|---|---|---|
| S6-T10 | `compiler/parser.py`: refatorar `_parse_program` para coleta em qualquer ordem | médio |
| S6-T11 | Implementar validação semântica em segundo passo (após coleta dos 3 blocos) | médio |
| S6-T12 | Lançar `ParseError` em bloco duplicado ou ausente | baixo |
| S6-T13 | Adicionar testes para ordens alternativas | baixo |
| S6-T14 | Atualizar `docs/language.md` — seção "Estrutura geral" | baixo |

---

## Definition of Done

- [ ] Todos os testes passam (pytest)
- [ ] Checklist de regressão executado (`examples/basic.gnj` e `examples/test.gnj` compilam corretamente)
- [ ] Memória atualizada em `ia/`
- [ ] `docs/language.md` reflete a gramática atual

---

## Pendências e resumo

(a preencher ao encerrar)
