# SPR-2026-09

- Status: concluída
- Data de início: 2026-05-02
- Data de fechamento: 2026-05-02
- Foco: B-016 — Extensão VS Code: suporte à sintaxe inline `@proc()` e keyword `when`
- Owner: agente + gresendesa (PO)

---

## Escopo

| Item | Título | Prioridade PO | Risco | Ordem |
|---|---|---|---|---|
| B-016 | Extensão VS Code: suporte à sintaxe inline `@proc()` e keyword `when` | 1 (crítica) | baixo | 1 |

---

## Tarefas técnicas

| Tarefa | Descrição | Risco |
|---|---|---|
| S9-T01 | `genjin.tmLanguage.json` — adicionar `when` ao grupo `keyword.control.genjin` | baixo |
| S9-T02 | `genjin.tmLanguage.json` — nova regra `inline-call` para `@proc()` | baixo |
| S9-T03 | `vscode-genjin/package.json` — bump de versão patch (0.1.0 → 0.1.1) | baixo |
| S9-T04 | Teste manual: abrir `examples/test.gnj` e verificar colorização | baixo |
| S9-T05 | Atualizar memória (`ia/`) | baixo |

**Risco da sprint: baixo (1.0)**

---

## Definition of Done

- [x] `when` destaca como keyword de controle em `.gnj`
- [x] `@proc()` tem `@` e nome da função destacados
- [x] JSON válido (sem regressões de sintaxe)
- [x] Versão da extensão incrementada para 0.1.1
- [x] Memória `ia/` atualizada

---

## Concluídos

- S9-T01: `genjin.tmLanguage.json` — `when` adicionado a `keyword.control.genjin`
- S9-T02: `genjin.tmLanguage.json` — regra `inline-call` criada para `@proc()`; incluída nos `patterns` raiz
- S9-T03: `vscode-genjin/package.json` — versão 0.1.0 → 0.1.1
- S9-T04: JSON validado (python3 json.load)
- S9-T05: Memória `ia/` atualizada

## Resumo

Sprint concluída. Extensão VS Code atualizada para cobrir a sintaxe da SPR-2026-08.
Alterações: +1 keyword (`when`), +1 regra de highlight (`inline-call` para `@proc()`), bump patch para 0.1.1.
Nenhuma regressão — JSON válido.
